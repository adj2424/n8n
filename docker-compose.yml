services:
  n8n:
    build: .
    ports:
      - '8080:8080'
    environment:
      - TZ=America/New_York
      - N8N_PORT=8080
      - N8N_RESTRICT_FILE_ACCESS_TO=/home/node/.n8n-files
      - NODES_EXCLUDE="[]"
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data:
